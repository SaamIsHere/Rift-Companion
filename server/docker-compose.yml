services:
  rift-server:
    build: .
    container_name: rift-server
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - DATA_DIR=/data
      - CRAWL_CONCURRENCY=6
      - CRON_HOUR=4
